'use strict';

const { GOOGLE_APPLICATION_CREDENTIALS, GOOGLE_CLOUD_FUNCTION, NODE_ENV } = process.env;

module.exports = {
  GOOGLE: {
    CLOUD_FUNCTION: GOOGLE_CLOUD_FUNCTION,
    CREDENTIALS: GOOGLE_APPLICATION_CREDENTIALS,
  },
  LOGGER: {
    LOGGING_LEVELS: ['error', 'warn', 'info', 'debug'],
    TRANSPORTS: {
      FILE: {
        ERROR_OPTIONS: {
          filename: 'error.log',
          level: 'error',
        },
        COMBINED_OPTIONS: {
          filename: 'combined.log',
        },
      },
    },
  },
  NODE_ENV,
};
