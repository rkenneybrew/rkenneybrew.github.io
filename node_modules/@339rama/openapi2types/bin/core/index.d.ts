import { WriteStream } from 'fs';
import * as Types from '../types';
export declare const parseV2types: (schema: Types.Schema, writeStream: WriteStream) => void;
export declare const parseV3types: (schema: Types.Schema, writeStream: WriteStream) => void;
