"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.logDiff = void 0;
const chalk_1 = __importDefault(require("chalk"));
const diff_1 = require("diff");
function logDiff(oldContent, newContent) {
    const diff = diff_1.diffLines(oldContent, newContent, {
        newlineIsToken: false,
        ignoreWhitespace: false
    });
    diff.forEach(part => {
        if (part.added) {
            part.value
                .split(/\r?\n/)
                .filter(line => line.trim())
                .map(line => console.log(chalk_1.default.green(`    +  ${line}`)));
        }
        if (part.removed) {
            part.value
                .split(/\r?\n/)
                .filter(line => line.trim())
                .map(line => console.log(chalk_1.default.red(`    -  ${line}`)));
        }
    });
}
exports.logDiff = logDiff;
