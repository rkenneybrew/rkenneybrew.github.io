// JavaScript Document

// Imports //
const express = require('express')
const app = express()
const port = 3000


// Static Files (HTML, IMG. CSS, etc.) //




// Listen on port 3000 //
app.listen(port, () => console.info(`Listening on port ${port}`))